<h3>Post Update Page</h3>
Post content:<p><%= post.content %></p>
<% if current_user.id = post.id || current_user.admin? || current_user.vip? %>
  <%= link_to "Edit Post", edit_post_path(post) %>
<% end %>
